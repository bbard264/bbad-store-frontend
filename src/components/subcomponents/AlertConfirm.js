import React from 'react';
import '../../styles/components/subcomponents/AlertConfirm.css';

export function Alert({ text }) {
  return <div>{text}</div>;
}

export function Confirm({ text }) {
  return <div>{text}</div>;
}
